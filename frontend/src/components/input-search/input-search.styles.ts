import { css } from '@emotion/css';

const root = css({
  width: '100%',
  display: 'flex',
  position: 'relative',
  alignItems: 'center',
});

const icon = css({
  position: 'absolute',
  color: '#999999',
  left: 10,
});

export default {
  root,
  icon,
};
