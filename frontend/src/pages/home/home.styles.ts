import { css } from '@emotion/css';

const root = css({
  width: '100%',
});

const content = css({
  width: '100%',
  padding: '20px 30px',
});

const contentTitle = css({
  color: '#777777',
  fontSize: 20,
  fontWeight: 600,
});

const list = css({
  paddingLeft: 10,
});

const item = css({
  color: '#666666',
});

export default {
  root,
  content,
  contentTitle,
  list,
  item,
};
