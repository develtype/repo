import { css } from '@emotion/css';

const root = css({
  width: 800,
  padding: '20px 30px',
});

const controlWrapper = css({
  display: 'flex',
  justifyContent: 'space-between',
});

export default {
  root,
  controlWrapper,
};
